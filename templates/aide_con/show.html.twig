{% extends 'base.html.twig' %}


{% block title %}Aide{% endblock %}

{% block content %}
    <h1>Aide</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ aide.id }}</td>
        </tr>
        <tr>
            <th>Sujet</th>
            <td>{{ aide.sujet }}</td>
        </tr>
        <tr>
            <th>Probleme</th>
            <td>{{ aide.probleme }}</td>
        </tr>
        <tr>
            <th>Mail</th>
            <td>{{ aide.mail }}</td>
        </tr>
        <tr>
            <th>commaintre</th>
            <td>
                {{ aidecom.commaintre }}
            </td>
        </tr>
        </tbody>
    </table>
<a href="{{ path('impp', {'id': aide.id}) }}" class="btn btn-primary"> imprimer </a></br>

    <a href="{{ path('aide_con_index') }}">back to list</a></br>

    <a href="{{ path('aide_con_edit', {'id': aide.id}) }}">edit</a>

    {{ include('aide_con/_delete_form.html.twig') }}
{% endblock %}
{% block footer %}

{% endblock %}