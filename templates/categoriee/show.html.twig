{% extends 'base.html.twig' %}

{% block title %}Categoriee{% endblock %}

{% block body %}
    <h1>Categoriee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categoriee.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ categoriee.numero }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ categoriee.lien }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categoriee.description }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ categoriee.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categoriee_index') }}">back to list</a>

    <a href="{{ path('categoriee_edit', {'id': categoriee.id}) }}">edit</a>

    {{ include('categoriee/_delete_form.html.twig') }}
{% endblock %}
