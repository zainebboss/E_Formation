{% extends 'base1.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCatégorie</th>
                <td>{{ categorie.idCatégorie }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorie.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_index') }}">back to list</a>

    <a href="{{ path('categorie_edit', {'id': categorie.idCatégorie}) }}">edit</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
