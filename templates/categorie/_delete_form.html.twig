<form method="post" action="{{ path('categorie_delete', {'id': categorie.idCatégorie}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie.idCatégorie) }}">
    <button class="btn">Delete</button>
</form>
