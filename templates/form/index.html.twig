{% extends 'back.html.twig' %}

{% block title %}Aide index{% endblock %}

{% block content %}
    <h1>Liste des problemes</h1>
    <form  method="post" action="{{ path('recherche') }}" >

        <input type="text" name="search" class="in"> <input type="submit" value="recherche" class="re">

    </form>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Sujet</th>
                <th>Probleme</th>
                <th>Mail</th>
                <th>commaintre</th>
                <th>reponse</th>

            </tr>
        </thead>
        <tbody>
        {% for aide in aides %}
            <tr>
                <td>{{ aide.id }}</td>
                <td>{{ aide.sujet }}</td>
                <td>{{ aide.probleme }}</td>
                <td>{{ aide.mail }}</td>
                <td>  {% for aidecoms in aidecom %}
                        {% if aidecoms.idSujet == aide.id %}
                            <p>{{ aidecoms.commaintre}}</p>

                        {% endif %}
                    {% endfor %}
                </td>
                <td style="width: 10%;"><a class="btn btn-warning" href="{{ path('aide_commaintre_new')}}"> reponse</a> </td>

            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


{% endblock %}
