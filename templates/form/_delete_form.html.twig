<form method="post" action="{{ path('form_delete', {'id': aide.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aide.id) }}">
    <button class="btn">Delete</button>
</form>
